import * as ut from "./ut"

export function substring(
  left: string,
  right: string,
  start: number,
  end: number
): string {
  return "cde"
}

ut.assert_equal(substring("abc", "def", 2, 5), "cde")
